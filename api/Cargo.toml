[package]
name = "colmap-openmvs-api"
version = "0.1.0"
authors = ["Yeicor <yeicor+no-reply@github.com>"]
edition = "2021"

[dependencies]
# Core serialization
serde = { version = "*", features = ["derive"] }
